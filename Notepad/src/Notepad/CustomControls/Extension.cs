namespace Notepad.CustomControls
{
    /// <summary>
    /// Enum define file's type. 
    /// </summary>
    public enum Extension
    {
        TextFile,
        Rtf,
        Doc,
        Code
    }
}